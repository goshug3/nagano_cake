<h1>商品詳細</h1>

<%= image_tag @item.get_image(200,200) %>

<h4>商品名</h4>
<%= @item.name %>

<h4>商品説明</h4>
<%= @item.introduction %>

<h4>ジャンル</h4>
<%= @item.genre.name %>

<h4>税込価格</h4> <br> <h4>(税抜価格)</h4>
<%= @item.add_tax_price.to_s(:delimited) %> (<%= @item.price.to_s(:delimited) %>)

<%= link_to "編集する", edit_admin_item_path(@item.id) %>
